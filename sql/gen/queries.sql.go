// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: queries.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteSecretAccess = `-- name: DeleteSecretAccess :execrows
DELETE FROM secret_access
WHERE secret_id = $1 AND user_id = $2
`

type DeleteSecretAccessParams struct {
	SecretID int64
	UserID   int64
}

func (q *Queries) DeleteSecretAccess(ctx context.Context, arg DeleteSecretAccessParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteSecretAccess, arg.SecretID, arg.UserID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const doesUserExist = `-- name: DoesUserExist :one
SELECT EXISTS(SELECT 1 FROM users WHERE subject = $1)
`

func (q *Queries) DoesUserExist(ctx context.Context, subject string) (bool, error) {
	row := q.db.QueryRow(ctx, doesUserExist, subject)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}

const hasKeys = `-- name: HasKeys :one
SELECT EXISTS(SELECT 1 FROM keys WHERE user_id = $1)
`

func (q *Queries) HasKeys(ctx context.Context, userID int64) (bool, error) {
	row := q.db.QueryRow(ctx, hasKeys, userID)
	var exists bool
	err := row.Scan(&exists)
	return exists, err
}

const insertKeys = `-- name: InsertKeys :one
INSERT INTO keys (user_id, password_id, type, key_material, public_key)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, user_id, password_id, type, key_material, public_key
`

type InsertKeysParams struct {
	UserID      int64
	PasswordID  pgtype.Int8
	Type        int32
	KeyMaterial []byte
	PublicKey   []byte
}

func (q *Queries) InsertKeys(ctx context.Context, arg InsertKeysParams) (Key, error) {
	row := q.db.QueryRow(ctx, insertKeys,
		arg.UserID,
		arg.PasswordID,
		arg.Type,
		arg.KeyMaterial,
		arg.PublicKey,
	)
	var i Key
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.PasswordID,
		&i.Type,
		&i.KeyMaterial,
		&i.PublicKey,
	)
	return i, err
}

const insertPassword = `-- name: InsertPassword :one
INSERT INTO passwords (hash, salt, iterations)
VALUES ($1, $2, $3)
RETURNING id, hash, salt, iterations
`

type InsertPasswordParams struct {
	Hash       []byte
	Salt       []byte
	Iterations int64
}

func (q *Queries) InsertPassword(ctx context.Context, arg InsertPasswordParams) (Password, error) {
	row := q.db.QueryRow(ctx, insertPassword, arg.Hash, arg.Salt, arg.Iterations)
	var i Password
	err := row.Scan(
		&i.ID,
		&i.Hash,
		&i.Salt,
		&i.Iterations,
	)
	return i, err
}

const insertSecret = `-- name: InsertSecret :one
INSERT INTO secrets (value, key, url, tags, user_id)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, value, key, url, tags, user_id, secret_sharing, envelope, created_at, updated_at
`

type InsertSecretParams struct {
	Value  []byte
	Key    string
	Url    string
	Tags   string
	UserID int64
}

func (q *Queries) InsertSecret(ctx context.Context, arg InsertSecretParams) (Secret, error) {
	row := q.db.QueryRow(ctx, insertSecret,
		arg.Value,
		arg.Key,
		arg.Url,
		arg.Tags,
		arg.UserID,
	)
	var i Secret
	err := row.Scan(
		&i.ID,
		&i.Value,
		&i.Key,
		&i.Url,
		&i.Tags,
		&i.UserID,
		&i.SecretSharing,
		&i.Envelope,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertSecretAccess = `-- name: InsertSecretAccess :exec
INSERT INTO secret_access (secret_id, user_id, encrypted_data_key, granted_by)
VALUES ($1, $2, $3, $4)
ON CONFLICT (secret_id, user_id) DO NOTHING
`

type InsertSecretAccessParams struct {
	SecretID         int64
	UserID           int64
	EncryptedDataKey []byte
	GrantedBy        int64
}

func (q *Queries) InsertSecretAccess(ctx context.Context, arg InsertSecretAccessParams) error {
	_, err := q.db.Exec(ctx, insertSecretAccess,
		arg.SecretID,
		arg.UserID,
		arg.EncryptedDataKey,
		arg.GrantedBy,
	)
	return err
}

const insertUser = `-- name: InsertUser :one
INSERT INTO users (subject, email, full_name)
VALUES ($1, $2, $3)
RETURNING id, subject, email, full_name, sharing_id, created_at, updated_at
`

type InsertUserParams struct {
	Subject  string
	Email    string
	FullName string
}

func (q *Queries) InsertUser(ctx context.Context, arg InsertUserParams) (User, error) {
	row := q.db.QueryRow(ctx, insertUser, arg.Subject, arg.Email, arg.FullName)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Subject,
		&i.Email,
		&i.FullName,
		&i.SharingID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const searchUsers = `-- name: SearchUsers :many
SELECT id, subject, email, full_name, sharing_id, created_at, updated_at
FROM users
WHERE users.id <> $1
  AND (LOWER(full_name) LIKE LOWER($2) || '%' OR LOWER(email) = LOWER($2))
  AND EXISTS (SELECT 1 FROM keys WHERE keys.user_id = users.id AND keys.public_key IS NOT NULL)
ORDER BY CASE WHEN LOWER(email) = LOWER($2) THEN 0 ELSE 1 END, full_name, users.id
LIMIT 10
`

type SearchUsersParams struct {
	ID    int64
	Lower string
}

func (q *Queries) SearchUsers(ctx context.Context, arg SearchUsersParams) ([]User, error) {
	rows, err := q.db.Query(ctx, searchUsers, arg.ID, arg.Lower)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Subject,
			&i.Email,
			&i.FullName,
			&i.SharingID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const selectAccessibleSecrets = `-- name: SelectAccessibleSecrets :many
SELECT s.id, s.value, s.key, s.url, s.tags, s.user_id, s.secret_sharing, s.envelope, s.created_at, s.updated_at, sa.encrypted_data_key, s.user_id = $1 AS owned
FROM secrets s
JOIN secret_access sa ON sa.secret_id = s.id
WHERE sa.user_id = $1
`

type SelectAccessibleSecretsRow struct {
	ID               int64
	Value            []byte
	Key              string
	Url              string
	Tags             string
	UserID           int64
	SecretSharing    pgtype.Int4
	Envelope         bool
	CreatedAt        pgtype.Timestamp
	UpdatedAt        pgtype.Timestamp
	EncryptedDataKey []byte
	Owned            bool
}

func (q *Queries) SelectAccessibleSecrets(ctx context.Context, userID int64) ([]SelectAccessibleSecretsRow, error) {
	rows, err := q.db.Query(ctx, selectAccessibleSecrets, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []SelectAccessibleSecretsRow
	for rows.Next() {
		var i SelectAccessibleSecretsRow
		if err := rows.Scan(
			&i.ID,
			&i.Value,
			&i.Key,
			&i.Url,
			&i.Tags,
			&i.UserID,
			&i.SecretSharing,
			&i.Envelope,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.EncryptedDataKey,
			&i.Owned,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const selectKeyMaterial = `-- name: SelectKeyMaterial :one
SELECT key_material
FROM keys
WHERE user_id = $1
`

func (q *Queries) SelectKeyMaterial(ctx context.Context, userID int64) ([]byte, error) {
	row := q.db.QueryRow(ctx, selectKeyMaterial, userID)
	var key_material []byte
	err := row.Scan(&key_material)
	return key_material, err
}

const selectKeys = `-- name: SelectKeys :one
SELECT id, user_id, password_id, type, key_material, public_key
FROM keys
WHERE user_id = $1
`

func (q *Queries) SelectKeys(ctx context.Context, userID int64) (Key, error) {
	row := q.db.QueryRow(ctx, selectKeys, userID)
	var i Key
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.PasswordID,
		&i.Type,
		&i.KeyMaterial,
		&i.PublicKey,
	)
	return i, err
}

const selectPassword = `-- name: SelectPassword :one
SELECT id, hash, salt, iterations
FROM passwords
WHERE id = $1
`

func (q *Queries) SelectPassword(ctx context.Context, id int64) (Password, error) {
	row := q.db.QueryRow(ctx, selectPassword, id)
	var i Password
	err := row.Scan(
		&i.ID,
		&i.Hash,
		&i.Salt,
		&i.Iterations,
	)
	return i, err
}

const selectPublicKey = `-- name: SelectPublicKey :one
SELECT id, public_key FROM keys WHERE user_id = $1 AND public_key IS NOT NULL
`

type SelectPublicKeyRow struct {
	ID        int64
	PublicKey []byte
}

func (q *Queries) SelectPublicKey(ctx context.Context, userID int64) (SelectPublicKeyRow, error) {
	row := q.db.QueryRow(ctx, selectPublicKey, userID)
	var i SelectPublicKeyRow
	err := row.Scan(&i.ID, &i.PublicKey)
	return i, err
}

const selectSecretForOwner = `-- name: SelectSecretForOwner :one
SELECT s.id, s.value, s.key, s.url, s.tags, s.user_id, s.secret_sharing, s.envelope, s.created_at, s.updated_at, sa.encrypted_data_key
FROM secrets s
LEFT JOIN secret_access sa ON sa.secret_id = s.id AND sa.user_id = s.user_id
WHERE s.id = $1 AND s.user_id = $2
`

type SelectSecretForOwnerParams struct {
	ID     int64
	UserID int64
}

type SelectSecretForOwnerRow struct {
	ID               int64
	Value            []byte
	Key              string
	Url              string
	Tags             string
	UserID           int64
	SecretSharing    pgtype.Int4
	Envelope         bool
	CreatedAt        pgtype.Timestamp
	UpdatedAt        pgtype.Timestamp
	EncryptedDataKey []byte
}

func (q *Queries) SelectSecretForOwner(ctx context.Context, arg SelectSecretForOwnerParams) (SelectSecretForOwnerRow, error) {
	row := q.db.QueryRow(ctx, selectSecretForOwner, arg.ID, arg.UserID)
	var i SelectSecretForOwnerRow
	err := row.Scan(
		&i.ID,
		&i.Value,
		&i.Key,
		&i.Url,
		&i.Tags,
		&i.UserID,
		&i.SecretSharing,
		&i.Envelope,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.EncryptedDataKey,
	)
	return i, err
}

const selectSecretRecipients = `-- name: SelectSecretRecipients :many
SELECT u.id, u.subject, u.email, u.full_name, u.sharing_id, u.created_at, u.updated_at
FROM users u
JOIN secret_access sa ON sa.user_id = u.id
JOIN secrets s ON s.id = sa.secret_id
WHERE s.id = $1 AND s.user_id = $2 AND u.id <> s.user_id
ORDER BY u.full_name, u.id
`

type SelectSecretRecipientsParams struct {
	ID     int64
	UserID int64
}

func (q *Queries) SelectSecretRecipients(ctx context.Context, arg SelectSecretRecipientsParams) ([]User, error) {
	rows, err := q.db.Query(ctx, selectSecretRecipients, arg.ID, arg.UserID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []User
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Subject,
			&i.Email,
			&i.FullName,
			&i.SharingID,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const selectSecrets = `-- name: SelectSecrets :many
SELECT id, value, key, url, tags, user_id, secret_sharing, envelope, created_at, updated_at
FROM secrets
WHERE user_id = $1 AND envelope = FALSE
`

func (q *Queries) SelectSecrets(ctx context.Context, userID int64) ([]Secret, error) {
	rows, err := q.db.Query(ctx, selectSecrets, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Secret
	for rows.Next() {
		var i Secret
		if err := rows.Scan(
			&i.ID,
			&i.Value,
			&i.Key,
			&i.Url,
			&i.Tags,
			&i.UserID,
			&i.SecretSharing,
			&i.Envelope,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const selectUserByName = `-- name: SelectUserByName :one
SELECT id, subject, email, full_name, sharing_id, created_at, updated_at
FROM users
WHERE subject = $1
`

func (q *Queries) SelectUserByName(ctx context.Context, subject string) (User, error) {
	row := q.db.QueryRow(ctx, selectUserByName, subject)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Subject,
		&i.Email,
		&i.FullName,
		&i.SharingID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const selectUserBySharingID = `-- name: SelectUserBySharingID :one
SELECT id, subject, email, full_name, sharing_id, created_at, updated_at FROM users WHERE sharing_id = $1
`

func (q *Queries) SelectUserBySharingID(ctx context.Context, sharingID pgtype.UUID) (User, error) {
	row := q.db.QueryRow(ctx, selectUserBySharingID, sharingID)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Subject,
		&i.Email,
		&i.FullName,
		&i.SharingID,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updatePublicKey = `-- name: UpdatePublicKey :exec
UPDATE keys SET public_key = $1 WHERE user_id = $2
`

type UpdatePublicKeyParams struct {
	PublicKey []byte
	UserID    int64
}

func (q *Queries) UpdatePublicKey(ctx context.Context, arg UpdatePublicKeyParams) error {
	_, err := q.db.Exec(ctx, updatePublicKey, arg.PublicKey, arg.UserID)
	return err
}

const updateSecret = `-- name: UpdateSecret :one
UPDATE secrets
SET value = $1,
    key   = $2,
    url   = $3,
    tags  = $4
WHERE user_id = $5
  AND id = $6
RETURNING id, value, key, url, tags, user_id, secret_sharing, envelope, created_at, updated_at
`

type UpdateSecretParams struct {
	Value  []byte
	Key    string
	Url    string
	Tags   string
	UserID int64
	ID     int64
}

func (q *Queries) UpdateSecret(ctx context.Context, arg UpdateSecretParams) (Secret, error) {
	row := q.db.QueryRow(ctx, updateSecret,
		arg.Value,
		arg.Key,
		arg.Url,
		arg.Tags,
		arg.UserID,
		arg.ID,
	)
	var i Secret
	err := row.Scan(
		&i.ID,
		&i.Value,
		&i.Key,
		&i.Url,
		&i.Tags,
		&i.UserID,
		&i.SecretSharing,
		&i.Envelope,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateSecretEnvelope = `-- name: UpdateSecretEnvelope :exec
UPDATE secrets
SET value = $1, key = '', url = '', tags = '', envelope = TRUE, updated_at = NOW()
WHERE id = $2 AND user_id = $3
`

type UpdateSecretEnvelopeParams struct {
	Value  []byte
	ID     int64
	UserID int64
}

func (q *Queries) UpdateSecretEnvelope(ctx context.Context, arg UpdateSecretEnvelopeParams) error {
	_, err := q.db.Exec(ctx, updateSecretEnvelope, arg.Value, arg.ID, arg.UserID)
	return err
}
